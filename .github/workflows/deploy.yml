name: Deploy Flask to EKS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ${{ secrets.AWS_REGION }}
      ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REPOSITORY

      - name: Build & Push Docker image
        run: |
          docker buildx create --use || true
          docker buildx build --platform linux/amd64 -t flask-eks-app --load ./app
          docker tag flask-eks-app:latest $ECR_REPOSITORY:latest
          docker push $ECR_REPOSITORY:latest

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: v1.29.0

      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region $AWS_REGION --name flask-eks-eks
          export KUBECONFIG=$HOME/.kube/config

      - name: Debug kubeconfig
        run: |
          echo "=== Kubeconfig Context ==="
          kubectl config current-context
          echo "=== Cluster Info ==="
          kubectl cluster-info || true

      - name: Validate cluster access
        run: |
          kubectl get nodes

      - name: Install helm & helmfile
        run: |
          curl -sSL -o helm.tar.gz https://get.helm.sh/helm-v3.14.0-linux-amd64.tar.gz
          tar -zxvf helm.tar.gz
          sudo mv linux-amd64/helm /usr/local/bin/helm

          curl -sSL -o helmfile.tgz https://github.com/helmfile/helmfile/releases/download/v0.155.0/helmfile_0.155.0_linux_amd64.tar.gz
          tar -zxvf helmfile.tgz
          chmod +x helmfile
          sudo mv helmfile /usr/local/bin/helmfile

          helm plugin install https://github.com/databus23/helm-diff || true

      - name: Deploy to EKS using Helmfile
        run: |
          cd helm
          helmfile apply